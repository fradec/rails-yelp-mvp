<h2>Add a new review for <%= @restaurant.name %></h2>

<%= simple_form_for [@restaurant, @review] do |f| %>

  <%= f.input :content %>
  <%= f.input :rating %>

  <%= f.submit %>

<% end %>

<hr>

<%= link_to "Back to the restaurant", restaurant_path(@restaurant) %>
